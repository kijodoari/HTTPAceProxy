# HTTPAceProxy Environment Variables
# Copy this file to .env and customize as needed

# ====== HTTPAceProxy Server Settings ======
# Host interface to bind to (0.0.0.0 for all interfaces)
ACEPROXY_HOST=0.0.0.0

# Port to listen on
ACEPROXY_PORT=8888

# ====== Ace Stream Engine Connection ======
# Where is your Ace Stream Engine running?

# Option 1: On the same machine as Docker (Mac/Windows with Docker Desktop)
ACESTREAM_HOST=host.docker.internal

# Option 2: On Linux host (uncomment and use your host IP)
# ACESTREAM_HOST=172.17.0.1

# Option 3: On another machine on your network (use that machine's IP)
# ACESTREAM_HOST=192.168.1.100

# Ace Stream Engine API port (default: 62062)
ACESTREAM_API_PORT=62062

# Ace Stream Engine HTTP port (default: 6878)
ACESTREAM_HTTP_PORT=6878

# ====== Connection Limits ======
# Maximum total client connections (default: 10)
# This limits the total number of clients that can connect simultaneously,
# regardless of which channel they're watching.
# Examples: Personal use: 10, Family: 25, Shared server: 100
MAX_CONNECTIONS=10

# Maximum different channels streaming simultaneously (default: 5)
# This limits how many DIFFERENT channels can stream at the same time.
# Important: Multiple clients watching the SAME channel only count as 1 channel.
# Examples: Personal use: 3-5, Family: 5-10, Shared server: 15-20
# Note: Each channel requires a dedicated AceStream connection.
MAX_CONCURRENT_CHANNELS=5

# For detailed configuration guide, see: CONNECTION-LIMITS.md

# ====== Plugin Control ======
# Control which plugins are enabled
# Options:
#   'all' - Enable all available plugins (default)
#   'plugin1,plugin2' - Enable only specified plugins (comma-separated)
#   '' - Disable all plugins
#
# Available plugins:
#   - newera: 322 sports channels from IPFS
#   - elcano: 71 curated sports channels from IPFS
#   - acepl: 1000+ channels from Acestream API
#   - stat: Real-time statistics dashboard
#   - statplugin: Channel browser with peer checking
#
# Examples:
#   ENABLED_PLUGINS=all                    # All plugins (default)
#   ENABLED_PLUGINS=newera,acepl,stat      # Only NewEra, AcePL and Stats
#   ENABLED_PLUGINS=stat,statplugin        # Only dashboards (no playlists)
#   ENABLED_PLUGINS=acepl                  # Only AcePL plugin
ENABLED_PLUGINS=all

# ====== Plugin Playlist URLs (Optional) ======
# Override default playlist URLs for NewEra and Elcano plugins
# If not set, plugins will use their default URLs
#
# NewEra default: https://ipfs.io/ipns/k2k4r8oqlcjxsritt5mczkcn4mmvcmymbqw7113fz2flkrerfwfps004/data/listas/lista_fuera_iptv.m3u
# Elcano default: https://acestream-ids.vercel.app/hashes_acestream.m3u
#
# Supported URL types:
#   - HTTPS: https://example.com/playlist.m3u
#   - IPFS: https://ipfs.io/ipns/... or https://ipfs.io/ipfs/...
#   - Local files: file:///path/to/playlist.m3u
#
# Examples:
#   NEWERA_PLAYLIST_URL=https://my-server.com/newera.m3u
#   ELCANO_PLAYLIST_URL=https://another-url.com/elcano.m3u
#
# Leave commented to use defaults:
# NEWERA_PLAYLIST_URL=
# ELCANO_PLAYLIST_URL=

# ====== Python Settings ======
PYTHONUNBUFFERED=1
